# docs/

Mintlify source for https://docs.instacloud.com. Navigation lives in `docs.json`; a page not listed there does not render. Internal planning notes live in `/plans`, not here. Preview: `cd docs && npx mint dev`.
