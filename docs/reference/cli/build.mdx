---
title: "insta build"
description: "Verify a source directory would build before deploying — local and offline."
---

Check whether a directory will build before you deploy it: detection plan, Dockerfile checks, static checks. No login needed, nothing is pushed; exits 1 when the verdict is failed.

```bash
insta build .
insta build . --explain
```

## Flags

{/* TODO: --explain, --port, --json */}

## What it checks

{/* TODO: detection plan, Dockerfile presence/EXPOSE, static checks */}
